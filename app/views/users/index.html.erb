<%= render 'shared/navbar' %>
<h1> Home page la</h1>

  <%= flash[:no_cart] %>
  <div class="container">
      <div class="row">
          <div class="col-5 m-auto">
            <% @games.reverse_each do |game| %>
              <%= render 'shared/card', game: game %>
            <% end %>
          </div>
      </div>
  </div>