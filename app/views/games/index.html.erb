<%= render 'shared/navbar' %>

<h1> Gamess </h1>



<%= form_for :search, url: root_path, method: "GET", html: { class: 'form-inline' } do |f| %>
  <%= f.input :title, input_html: {value: ""} %>   
  <%= f.submit "Search", class: "btn btn-primary" %>
<% end %>



<h3>
  <% @games.each do |game| %>
    <p>$<%= game.cost / 100 %> </p>
    <%= game.title %>
  <% end %>




</h3>